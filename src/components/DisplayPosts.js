import React from "react";

const DisplayPosts = ({ post }) => {
  //   console.log(post);
  const { userId, title, body, id } = post;
  return <div>{/* <h2>{body}</h2> */}</div>;
};

export default DisplayPosts;
